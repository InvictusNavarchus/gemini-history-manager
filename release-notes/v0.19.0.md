# Release v0.19.0

This release introduces real-time dashboard updates, improved tool detection, and migrates the project to Bun for faster builds and development.

### ✨ New Features

- **Real-Time Dashboard Updates**: Dashboard and popup now automatically refresh when new conversations are tracked, eliminating the need for manual refreshes (#229)
- **Tool Detection**: Added separate `tool` field to distinguish feature tools (Deep Research, Canvas, etc.) from AI models (#228)

### 🐛 Bug Fixes

- Fixed model detector compatibility with Gemini's new UI structure (November 2025) (#227)
- Fixed null pointer errors when extracting tool labels with empty text content

### 🔧 Chores

- **Migrated to Bun**: Replaced pnpm with Bun for package management and script execution (#221)
- Pinned Bun version to 1.3.3 for reproducible builds
- Added Node.js engine version requirement
- Used `node:` prefix for built-in module imports for security best practices (#230)
- Added pre-flight checks to the release process to catch issues before building

### ♻️ Refactoring

- **Release System Overhaul**: Consolidated build utilities, ported checksum comparison from Python to JavaScript, and removed legacy scripts (#226)
- Simplified release workflow to PR-based only for better CI integration (#231)
- Consolidated shared utilities into `scripts/lib/utils.js`
- **Schema Versioning**: History entries now include schema version metadata for future migration support

### 📄 Documentation

- Added Firefox AMO installation link to README (#219)
- Added comprehensive release notes guidelines
- Updated documentation to reflect Bun migration
