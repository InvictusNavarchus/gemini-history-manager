# Release v0.19.0

This release introduces real-time dashboard updates and ensures compatibility with the latest Gemini UI update.

### ✨ New Features

- **Real-Time Dashboard Updates**: Dashboard and popup now automatically refresh when new conversations are tracked, eliminating the need for manual refreshes
- **Tool Detection**: Added separate `tool` field to distinguish feature tools (Deep Research, Canvas, etc.) from AI models

### 🐛 Bug Fixes

- Fixed model detector compatibility with Gemini's new UI structure
